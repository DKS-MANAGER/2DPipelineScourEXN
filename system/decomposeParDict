FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    location    "system";
    object      decomposeParDict;
}
// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * //

numberOfSubdomains  8;

method              scotch;

// NOTE: singleProcessorFaceSets constraint for 'bottom' is intentionally
// omitted here. It must only be added AFTER snappyHexMesh has run and the
// bottom faceSet exists in constant/polyMesh/sets/. Re-add it before
// running sedExnerFoam in parallel.

// ************************************************************************* //
