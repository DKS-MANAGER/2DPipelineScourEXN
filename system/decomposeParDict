FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    location    "system";
    object      decomposeParDict;
}
// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * //

numberOfSubdomains  8;

method              scotch;

// v2412: singleProcessorFaceSets 'sets' takes a list of (faceSetName processor) tuples.
constraints
{
    seabedConstraint
    {
        type        singleProcessorFaceSets;
        sets        ((seabed 0));
    }
}

// ************************************************************************* //
